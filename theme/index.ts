import { colors } from "./colors";

export const theme = {
    colors,
    // Add spacing, typography, borderRadius here later if needed to be exposed to JS
    // For NativeWind, most will be in tailwind.config.js
};

export * from "./colors";
